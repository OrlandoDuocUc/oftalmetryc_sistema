████████████████████████████████████████████████████████████████████████████████
██                                                                            ██
██  ██████   █████   ██████  ███████     ██████  ███████     ██████   █████   ██
██  ██   ██ ██   ██ ██       ██          ██   ██ ██          ██   ██ ██   ██  ██
██  ██████  ███████  ██████  █████       ██   ██ █████       ██   ██ ███████  ██
██  ██   ██ ██   ██       ██ ██          ██   ██ ██          ██   ██ ██   ██  ██
██  ██████  ██   ██  ██████  ███████     ██████  ███████     ██████  ██   ██  ██
██                                                                            ██
████████████████████████████████████████████████████████████████████████████████

 ██████ ██████  ███    ███ ██████  ██      ███████ ████████  █████  
██      ██    ██ ████  ████ ██   ██ ██      ██         ██    ██   ██ 
██      ██    ██ ██ ████ ██ ██████  ██      █████      ██    ███████ 
██      ██    ██ ██  ██  ██ ██      ██      ██         ██    ██   ██ 
 ██████  ██████  ██      ██ ██      ███████ ███████    ██    ██   ██ 

════════════════════════════════════════════════════════════════════════════════════
NUEVA BASE DE DATOS POSTGRESQL - ESQUEMA COMPLETO
PROYECTO: ÓPTICA MAIPÚ - SISTEMA INTEGRADO COMERCIAL + MÉDICO
FECHA: 03/10/2025
NOMBRE SUGERIDO: optica_maipu_complete
════════════════════════════════════════════════════════════════════════════════════

🏗️ ARQUITECTURA DE BASE DE DATOS:
═══════════════════════════════════════════════════════════════════════════════════

📊 MÓDULO 1: SISTEMA COMERCIAL (BASE FUNCIONAL)
═══════════════════════════════════════════════════════════════════════════════════

1️⃣ TABLA: roles
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE roles (
    rol_id SERIAL PRIMARY KEY,
    nombre VARCHAR(50) UNIQUE NOT NULL,
    descripcion TEXT,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    estado BOOLEAN DEFAULT TRUE
);

2️⃣ TABLA: usuarios  
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE usuarios (
    usuario_id SERIAL PRIMARY KEY,
    rol_id INTEGER REFERENCES roles(rol_id),
    username VARCHAR(80) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    nombre VARCHAR(100) NOT NULL,
    ap_pat VARCHAR(100) NOT NULL,
    ap_mat VARCHAR(100),
    email VARCHAR(120) UNIQUE NOT NULL,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    estado BOOLEAN DEFAULT TRUE
);

3️⃣ TABLA: clientes
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE clientes (
    cliente_id SERIAL PRIMARY KEY,
    nombres VARCHAR(100) NOT NULL,
    ap_pat VARCHAR(100) NOT NULL,
    ap_mat VARCHAR(100),
    rut VARCHAR(20) UNIQUE NOT NULL,
    email VARCHAR(120),
    telefono VARCHAR(20),
    direccion TEXT,
    fecha_nacimiento DATE,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    estado BOOLEAN DEFAULT TRUE
);

4️⃣ TABLA: productos
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE productos (
    producto_id SERIAL PRIMARY KEY,
    nombre VARCHAR(200) NOT NULL,
    descripcion TEXT,
    stock INTEGER DEFAULT 0,
    precio_unitario DECIMAL(10,2) NOT NULL,
    categoria VARCHAR(100),
    marca VARCHAR(100),
    sku VARCHAR(50) UNIQUE,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    estado BOOLEAN DEFAULT TRUE
);

5️⃣ TABLA: ventas
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE ventas (
    venta_id SERIAL PRIMARY KEY,
    cliente_id INTEGER REFERENCES clientes(cliente_id),
    usuario_id INTEGER REFERENCES usuarios(usuario_id),
    fecha_venta TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total DECIMAL(10,2) NOT NULL,
    descuento DECIMAL(5,2) DEFAULT 0,
    metodo_pago VARCHAR(50),
    observaciones TEXT,
    estado VARCHAR(20) DEFAULT 'completada'
);

6️⃣ TABLA: detalle_ventas
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE detalle_ventas (
    detalle_id SERIAL PRIMARY KEY,
    venta_id INTEGER REFERENCES ventas(venta_id),
    producto_id INTEGER REFERENCES productos(producto_id),
    cantidad INTEGER NOT NULL,
    precio_unitario DECIMAL(10,2) NOT NULL,
    subtotal DECIMAL(10,2) NOT NULL
);

═══════════════════════════════════════════════════════════════════════════════════

🏥 MÓDULO 2: SISTEMA MÉDICO OFTALMOLÓGICO (NUEVA INTEGRACIÓN)
═══════════════════════════════════════════════════════════════════════════════════

7️⃣ TABLA: pacientes_medicos
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE pacientes_medicos (
    paciente_medico_id SERIAL PRIMARY KEY,
    cliente_id INTEGER REFERENCES clientes(cliente_id), -- CONEXIÓN CON MÓDULO COMERCIAL
    numero_ficha VARCHAR(20) UNIQUE NOT NULL,
    antecedentes_medicos TEXT,
    antecedentes_oculares TEXT,
    alergias TEXT,
    medicamentos_actuales TEXT,
    contacto_emergencia VARCHAR(100),
    telefono_emergencia VARCHAR(20),
    fecha_registro TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    estado BOOLEAN DEFAULT TRUE
);

8️⃣ TABLA: fichas_clinicas
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE fichas_clinicas (
    ficha_id SERIAL PRIMARY KEY,
    paciente_medico_id INTEGER REFERENCES pacientes_medicos(paciente_medico_id),
    usuario_id INTEGER REFERENCES usuarios(usuario_id), -- MÉDICO QUE ATIENDE
    numero_consulta VARCHAR(20) UNIQUE NOT NULL,
    fecha_consulta TIMESTAMP NOT NULL,
    motivo_consulta TEXT,
    historia_actual TEXT,
    
    -- AGUDEZA VISUAL OJO DERECHO
    av_od_sc VARCHAR(20),           -- Sin corrección
    av_od_cc VARCHAR(20),           -- Con corrección  
    av_od_ph VARCHAR(20),           -- Pinhole
    av_od_cerca VARCHAR(20),        -- Visión cercana
    
    -- AGUDEZA VISUAL OJO IZQUIERDO
    av_oi_sc VARCHAR(20),
    av_oi_cc VARCHAR(20),
    av_oi_ph VARCHAR(20),
    av_oi_cerca VARCHAR(20),
    
    -- REFRACCIÓN OJO DERECHO
    esfera_od VARCHAR(10),
    cilindro_od VARCHAR(10),
    eje_od VARCHAR(10),
    adicion_od VARCHAR(10),
    
    -- REFRACCIÓN OJO IZQUIERDO
    esfera_oi VARCHAR(10),
    cilindro_oi VARCHAR(10),
    eje_oi VARCHAR(10),
    adicion_oi VARCHAR(10),
    
    -- DATOS GENERALES REFRACCIÓN
    distancia_pupilar VARCHAR(10),
    tipo_lente VARCHAR(50),
    
    estado VARCHAR(20) DEFAULT 'en_proceso',
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

9️⃣ TABLA: biomicroscopia
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE biomicroscopia (
    biomicroscopia_id SERIAL PRIMARY KEY,
    ficha_id INTEGER REFERENCES fichas_clinicas(ficha_id),
    
    -- OJO DERECHO
    parpados_od TEXT,
    conjuntiva_od TEXT,
    cornea_od TEXT,
    camara_anterior_od TEXT,
    iris_od TEXT,
    pupila_od_mm VARCHAR(10),
    pupila_od_reaccion VARCHAR(20),
    cristalino_od TEXT,
    
    -- OJO IZQUIERDO  
    parpados_oi TEXT,
    conjuntiva_oi TEXT,
    cornea_oi TEXT,
    camara_anterior_oi TEXT,
    iris_oi TEXT,
    pupila_oi_mm VARCHAR(10),
    pupila_oi_reaccion VARCHAR(20),
    cristalino_oi TEXT,
    
    observaciones_generales TEXT,
    fecha_examen TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

🔟 TABLA: fondo_ojo
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE fondo_ojo (
    fondo_ojo_id SERIAL PRIMARY KEY,
    ficha_id INTEGER REFERENCES fichas_clinicas(ficha_id),
    
    -- OJO DERECHO
    disco_optico_od TEXT,
    macula_od TEXT,
    vasos_od TEXT,
    retina_periferica_od TEXT,
    
    -- OJO IZQUIERDO
    disco_optico_oi TEXT,
    macula_oi TEXT,
    vasos_oi TEXT,
    retina_periferica_oi TEXT,
    
    observaciones TEXT,
    fecha_examen TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

1️⃣1️⃣ TABLA: presion_intraocular
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE presion_intraocular (
    pio_id SERIAL PRIMARY KEY,
    ficha_id INTEGER REFERENCES fichas_clinicas(ficha_id),
    pio_od VARCHAR(10),                    -- Presión OD en mmHg
    pio_oi VARCHAR(10),                    -- Presión OI en mmHg
    metodo_medicion VARCHAR(50),           -- Goldmann, Neumática, etc.
    hora_medicion TIME,
    observaciones TEXT,
    fecha_medicion TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

1️⃣2️⃣ TABLA: campos_visuales
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE campos_visuales (
    campo_visual_id SERIAL PRIMARY KEY,
    ficha_id INTEGER REFERENCES fichas_clinicas(ficha_id),
    tipo_campo VARCHAR(50),                -- Confrontación, Humphrey, Octopus, etc.
    resultado_od TEXT,
    resultado_oi TEXT,
    interpretacion TEXT,
    fecha_examen TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

1️⃣3️⃣ TABLA: diagnosticos
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE diagnosticos (
    diagnostico_id SERIAL PRIMARY KEY,
    ficha_id INTEGER REFERENCES fichas_clinicas(ficha_id),
    diagnostico_principal TEXT NOT NULL,
    diagnosticos_secundarios TEXT,
    cie_10_principal VARCHAR(10),          -- Código CIE-10
    cie_10_secundarios TEXT,
    severidad VARCHAR(20),                 -- Leve, Moderado, Severo
    observaciones TEXT,
    fecha_diagnostico TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

1️⃣4️⃣ TABLA: tratamientos
────────────────────────────────────────────────────────────────────────────────────
CREATE TABLE tratamientos (
    tratamiento_id SERIAL PRIMARY KEY,
    ficha_id INTEGER REFERENCES fichas_clinicas(ficha_id),
    medicamentos TEXT,
    tratamiento_no_farmacologico TEXT,
    recomendaciones TEXT,
    plan_seguimiento TEXT,
    proxima_cita DATE,
    urgencia_seguimiento VARCHAR(20),      -- Rutinario, Preferente, Urgente
    fecha_tratamiento TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

═══════════════════════════════════════════════════════════════════════════════════

🔗 RELACIONES DE LA BASE DE DATOS:
═══════════════════════════════════════════════════════════════════════════════════

MÓDULO COMERCIAL ↔ MÓDULO MÉDICO:
┌─────────────────┐       ┌─────────────────┐
│    CLIENTES     │◄──────┤ PACIENTES_MEDICOS│
│ cliente_id (PK) │  1:1  │ cliente_id (FK) │
└─────────────────┘       └─────────────────┘

USUARIOS (MÉDICOS) → FICHAS CLÍNICAS:
┌─────────────────┐       ┌─────────────────┐
│    USUARIOS     │◄──────┤ FICHAS_CLINICAS │
│ usuario_id (PK) │  1:N  │ usuario_id (FK) │
└─────────────────┘       └─────────────────┘

FICHA CLÍNICA → EXÁMENES:
┌─────────────────┐       ┌─────────────────┐
│ FICHAS_CLINICAS │◄──────┤ BIOMICROSCOPIA  │
│ ficha_id (PK)   │  1:1  │ ficha_id (FK)   │
└─────────────────┘       └─────────────────┘
         │                ┌─────────────────┐
         └────────────────┤ FONDO_OJO       │
         │           1:1  │ ficha_id (FK)   │
         │                └─────────────────┘
         │                ┌─────────────────┐
         └────────────────┤ PRESION_INTRAOC │
         │           1:1  │ ficha_id (FK)   │
         │                └─────────────────┘
         │                ┌─────────────────┐
         └────────────────┤ DIAGNOSTICOS    │
                     1:1  │ ficha_id (FK)   │
                          └─────────────────┘

═══════════════════════════════════════════════════════════════════════════════════

📋 DATOS INICIALES REQUERIDOS:
═══════════════════════════════════════════════════════════════════════════════════

ROLES DEL SISTEMA:
├── 1. Administrador (Acceso total)
├── 2. Médico Oftalmólogo (Módulo médico + consultas)
├── 3. Vendedor (Módulo comercial)
├── 4. Recepcionista (Registro pacientes + citas)
└── 5. Supervisor (Reportes + supervisión)

USUARIO ADMINISTRADOR INICIAL:
├── Username: admin
├── Password: [A DEFINIR]
├── Rol: Administrador
└── Email: admin@opticamaipu.cl

═══════════════════════════════════════════════════════════════════════════════════

🚀 VENTAJAS DE ESTA ARQUITECTURA:
═══════════════════════════════════════════════════════════════════════════════════

✅ COMPATIBILIDAD TOTAL: Base comercial funcional preserved
✅ INTEGRACIÓN PERFECTA: Cliente = Paciente médico
✅ ESCALABILIDAD: Módulos independientes pero conectados  
✅ RENDIMIENTO: Tablas especializadas por función
✅ TRAZABILIDAD: Histórico completo médico y comercial
✅ FLEXIBILIDAD: Fácil expansión futura
✅ SEGURIDAD: Roles específicos por módulo

═══════════════════════════════════════════════════════════════════════════════════

📊 ESTADÍSTICAS DE LA NUEVA BD:
═══════════════════════════════════════════════════════════════════════════════════

TOTAL TABLAS: 14
├── Módulo Comercial: 6 tablas (BASE FUNCIONAL)
├── Módulo Médico: 8 tablas (NUEVA INTEGRACIÓN)
└── Campos Oftalmológicos: 85+ campos especializados

TOTAL CAMPOS MÉDICOS MAPEADOS:
├── Agudeza Visual: 8 campos (OD + OI)
├── Refracción: 10 campos (esfera, cilindro, eje, adición)
├── Biomicroscopía: 16 campos (párpados, conjuntiva, córnea, etc.)
├── Fondo de Ojo: 8 campos (disco, mácula, vasos, retina)
├── Presión Intraocular: 5 campos
├── Campos Visuales: 4 campos
├── Diagnósticos: 6 campos
└── Tratamientos: 6 campos

═══════════════════════════════════════════════════════════════════════════════════

🎯 NOMBRE SUGERIDO PARA LA BASE DE DATOS:
═══════════════════════════════════════════════════════════════════════════════════

📚 NOMBRE: optica_maipu_complete
📍 TIPO: PostgreSQL 15+
🔧 ENCODING: UTF8
🌐 LOCALE: es_CL.UTF-8
🔐 OWNER: postgres (desarrollo) / app_user (producción)

═══════════════════════════════════════════════════════════════════════════════════

✅ ESTE ESQUEMA ESTÁ LISTO PARA:
═══════════════════════════════════════════════════════════════════════════════════

1. 🔧 Mantener toda la funcionalidad comercial actual
2. 🏥 Integrar el módulo médico completo
3. 🎨 Conectar con los HTML existentes
4. 📊 Soportar los 85+ campos oftalmológicos
5. 🔗 Unificar cliente comercial con paciente médico
6. 📈 Escalar a futuras funcionalidades

═══════════════════════════════════════════════════════════════════════════════════

❓ ¿APRUEBAS ESTE ESQUEMA PARA PROCEDER CON LA CREACIÓN?

✅ SÍ - Crear base de datos "optica_maipu_complete"
❌ NO - Revisar/modificar esquema
🔧 MODIFICAR - Ajustar campos específicos

════════════════════════════════════════════════════════════════════════════════════