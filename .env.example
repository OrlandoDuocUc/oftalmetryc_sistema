# Variables de entorno para desarrollo
FLASK_ENV=development
SECRET_KEY=dev-secret-key-change-in-production
DATABASE_URL=postgresql://postgres:password@localhost:5432/optica_db
PORT=5000
HOST=0.0.0.0

# Para producción en Render, estas variables se configuran automáticamente:
# FLASK_ENV=production
# SECRET_KEY=tu-clave-secreta-segura
# DATABASE_URL=postgresql://...  (proporcionada por Render)
# PORT=10000  (proporcionado por Render)